# Optional: enables LLM planning/synthesis (uses structured extraction fallback if unset)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Agent safety limits
AGENT_MAX_STEPS=25
AGENT_MAX_ITERATIONS=25
